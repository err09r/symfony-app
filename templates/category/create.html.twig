{% extends "./base.html.twig" %}

{% block title %}New category | Create your category{% endblock %}

{% block body %}
    <div class="p-16 pt-10">

        <div class="bg-white p-16 shadow-xl">

            <h1 class="text-5xl font-bold text-center pb-8">
                Create a category
            </h1>

            {{ form_start(category_form) }}

                {{ form_widget(category_form) }}
                <div class="flex justify-end">
                    <button    
                        type="submit"
                        class="w-1/4 uppercase border border-zinc-700 border-2 bg-white text-zinc-700 text-center py-4 px-8 text-lg font-medium transition duration-300 transition-all hover:bg-zinc-700 hover:text-white">
                            Create
                    </button>
                </div>
                
            {{ form_end(category_form) }}
        
        </div>

    </div>
{% endblock %}